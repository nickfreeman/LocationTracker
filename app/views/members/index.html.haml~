-#  This file is app/views/members/index.html.haml

%h1 Member to Member Query

%table#members
  %thead
    %tr
      %th Name
      %th Email
      %th Role

  %tbody
    - @members.each do |member|
      %tr
        %td= member.name
        %td= member.email
        %td= member.role

= form_tag "email", :method => :get, :id => 'send_form' do 
  = submit_tag 'Send Request', :id => 'request_sent'
